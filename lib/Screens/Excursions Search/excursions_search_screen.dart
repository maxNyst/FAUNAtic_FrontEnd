import 'package:flutter/material.dart';

class ExcursionsSearchScreen extends StatelessWidget {
  const ExcursionsSearchScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}